# Practices

Reusable project operating practices for long-running Codex and GitHub workflows.

These practices capture generic operating doctrine: milestone boundaries, validation, non-destructive git, local-first workflow, context routing, and owner approval gates. They do not contain app code, secrets, credentials, private project doctrine, or repo-specific URLs.
